version: '3'
services:
  nginx:
    build: ./nginx 
    ports: 
      - "8080:80"
    volumes: 
      - ./:/app
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
  
  php: ./php
    build: ./php

  

       